server:
  port: 8085

spring:
  application:
    name: order-service
  datasource:
    username: 'bgnat'
    password: 'password'
    url: jdbc:postgresql://localhost:5435/orders
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

microservice:
  inventory:
    url:
      get: http://localhost:8086/api/v1/inventory