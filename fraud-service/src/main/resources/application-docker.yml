# FRAUD-SERVICE
spring:
  datasource:
    url: jdbc:postgresql://postgres:5435/fraud-service
    driverClassName: org.postgresql.Driver
    username: 'bgnat'
    password: 'password'

management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@discovery-server:8761/eureka