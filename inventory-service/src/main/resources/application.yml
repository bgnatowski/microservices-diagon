server:
  port: 0

spring:
  application:
    name: inventory-service
  datasource:
    username: 'bgnat'
    password: 'password'
    url: jdbc:postgresql://localhost:5436/inventory
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka
